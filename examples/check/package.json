{
  "name": "resultar-check-example",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node ../../packages/check/dist/cli.js --project tsconfig.json",
    "check:clean": "node ../../packages/check/dist/cli.js --project tsconfig.clean.json",
    "check:json": "node ../../packages/check/dist/cli.js --project tsconfig.json --json",
    "prepare": "pnpm --filter resultar build && pnpm --filter resultar-check build && pnpm --filter resultar-check native:build:current",
    "smoke": "pnpm run prepare && tsx scripts/smoke.ts"
  },
  "dependencies": {
    "resultar": "workspace:*",
    "resultar-check": "workspace:*"
  },
  "devDependencies": {
    "tsx": "catalog:"
  }
}
