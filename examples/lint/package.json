{
  "name": "resultar-check-compat-example",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "resultar-check",
    "prepare": "pnpm --filter resultar build && pnpm --filter resultar-check build",
    "smoke": "tsx scripts/smoke.ts",
    "smoke:vite": "pnpm run smoke"
  },
  "dependencies": {
    "resultar": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "resultar-check": "workspace:*",
    "tsx": "catalog:",
    "typescript-7": "catalog:",
    "vite-plus": "catalog:"
  }
}
