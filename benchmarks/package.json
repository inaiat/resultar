{
  "name": "resultar-benchmarks",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "pnpm --filter resultar build && node --import tsx resultar-vs-neverthrow.ts",
    "bench:check-cli": "pnpm run bench:check-native",
    "bench:check-native": "pnpm --filter resultar-check build && pnpm --filter resultar-check native:build && node --import tsx resultar-check-native.ts",
    "bench:neverthrow": "pnpm run bench",
    "bench:stable": "pnpm --filter resultar build && node --import tsx resultar-vs-stable.ts"
  },
  "dependencies": {
    "resultar": "workspace:*",
    "resultar-check": "workspace:*"
  },
  "devDependencies": {
    "cronometro": "^6.0.3",
    "neverthrow": "^8.2.0",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
