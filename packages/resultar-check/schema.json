{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/inaiat/resultar/main/packages/resultar-check/schema.json",
  "title": "Resultar Check tsconfig schema",
  "description": "Schema extension for configuring the resultar-check TypeScript language-service plugin in tsconfig.json.",
  "type": "object",
  "allowTrailingCommas": true,
  "additionalProperties": true,
  "properties": {
    "compilerOptions": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "plugins": { "type": "array", "items": { "$ref": "#/definitions/languageServicePlugin" } }
      }
    }
  },
  "definitions": {
    "languageServicePlugin": {
      "type": "object",
      "additionalProperties": true,
      "anyOf": [
        { "$ref": "#/definitions/resultarCheckPlugin" },
        { "type": "object", "additionalProperties": true }
      ],
      "allOf": [
        {
          "if": {
            "required": ["name"],
            "properties": { "name": { "$ref": "#/definitions/resultarPluginName" } }
          },
          "then": { "$ref": "#/definitions/resultarCheckPlugin" }
        }
      ]
    },
    "resultarCheckPlugin": {
      "type": "object",
      "additionalProperties": true,
      "required": ["name"],
      "properties": {
        "name": {
          "$ref": "#/definitions/resultarPluginName",
          "description": "Resultar language-service plugin package name."
        },
        "noDiscard": {
          "$ref": "#/definitions/ruleSeverity",
          "default": "error",
          "description": "Require Result and ResultAsync values to be handled or explicitly discarded."
        },
        "noDiscardMode": {
          "type": "string",
          "enum": ["must-use", "direct"],
          "default": "must-use",
          "description": "Controls no-discard analysis. must-use tracks assigned Resultar values until consumed; direct only reports discarded expressions."
        },
        "noTaggedErrorConstructorOverride": {
          "$ref": "#/definitions/ruleSeverity",
          "default": "warning",
          "description": "Keep the generated tagged-error constructor intact."
        },
        "noTryCatchInSafeTry": {
          "$ref": "#/definitions/ruleSeverity",
          "default": "warning",
          "description": "Avoid raw try/catch inside safeTry generators."
        },
        "noUnsafeAwait": {
          "$ref": "#/definitions/ruleSeverity",
          "default": "off",
          "description": "Require raw Promise awaits in Resultar async contexts to use Resultar boundaries."
        },
        "noUnsafeAwaitIgnoreCalls": {
          "type": "array",
          "items": { "$ref": "#/definitions/callPath" },
          "default": [],
          "description": "Exact dot-separated call paths that noUnsafeAwait should ignore, such as fastify.after."
        },
        "noUnsafeAwaitMode": {
          "type": "string",
          "enum": ["resultar-context", "all"],
          "default": "resultar-context",
          "description": "Controls where noUnsafeAwait applies. resultar-context checks Resultar async contexts and safeTry bodies; all also reports framework and bootstrap awaits."
        },
        "noUselessRecovery": {
          "$ref": "#/definitions/ruleSeverity",
          "default": "warning",
          "description": "Flag recovery calls on Result<T, never> and ResultAsync<T, never>."
        },
        "preferAndThen": {
          "$ref": "#/definitions/ruleSeverity",
          "default": "warning",
          "description": "Prefer andThen or asyncAndThen when map returns a Resultar value."
        },
        "preferMapErr": {
          "$ref": "#/definitions/ruleSeverity",
          "default": "warning",
          "description": "Prefer mapErr when orElse only returns another Err."
        },
        "preferTaggedError": {
          "$ref": "#/definitions/ruleSeverity",
          "default": "warning",
          "description": "Prefer createTaggedError over plain Error subclasses or err(new Error(...))."
        },
        "taggedErrorNameMatch": {
          "$ref": "#/definitions/ruleSeverity",
          "default": "warning",
          "description": "Require createTaggedError({ name }) to match the class name."
        },
        "typedCatchMapper": {
          "$ref": "#/definitions/ruleSeverity",
          "default": "warning",
          "description": "Require catch conversion helpers to map caught values to typed errors."
        },
        "unsafeResultTypeAssertion": {
          "$ref": "#/definitions/ruleSeverity",
          "default": "warning",
          "description": "Prevent assertions that narrow Resultar error channels."
        },
        "yieldStarInSafeTry": {
          "$ref": "#/definitions/ruleSeverity",
          "default": "warning",
          "description": "Require yield* for Resultar values inside safeTry."
        }
      }
    },
    "resultarPluginName": { "type": "string", "enum": ["resultar-check", "resultar-lint"] },
    "callPath": {
      "type": "string",
      "pattern": "^[A-Za-z_$][A-Za-z0-9_$]*(\\.[A-Za-z_$][A-Za-z0-9_$]*)+$"
    },
    "ruleSeverity": {
      "type": "string",
      "enum": ["error", "warning", "suggestion", "message", "off"]
    }
  }
}
