# Resultar Lint

Deprecated compatibility wrapper for `resultar-check`.

New projects should install `resultar-check` directly:

```sh
pnpm add -D resultar-check typescript-7@npm:typescript@rc
```

Then run:

```sh
resultar-check -p tsconfig.json --noEmit
```
