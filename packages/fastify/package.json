{
  "name": "resultar-fastify",
  "version": "0.1.0",
  "description": "Native Fastify services and request resource scopes for Resultar DI.",
  "keywords": [
    "dependency-injection",
    "fastify",
    "resultar"
  ],
  "homepage": "https://github.com/inaiat/resultar",
  "bugs": {
    "url": "https://github.com/inaiat/resultar/issues"
  },
  "license": "MIT",
  "author": "inaiat@gmail.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/inaiat/resultar.git",
    "directory": "packages/fastify"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "type": "module",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "analyze": "fallow",
    "build": "vp pack",
    "check": "vp check",
    "check:full": "pnpm run build && pnpm run check && pnpm run test:cov",
    "fmt": "vp fmt",
    "fmt:check": "vp fmt --check",
    "prepare": "vp config || true",
    "prepublishOnly": "vp pack",
    "smoke:package": "pnpm run build && node --import tsx scripts/smoke-package.ts",
    "test": "vp test",
    "test:watch": "vp test watch",
    "test:ui": "vp test --ui --watch",
    "test:cov": "vp test run --coverage"
  },
  "dependencies": {
    "fastify-plugin": "catalog:",
    "resultar": "workspace:^",
    "resultar-di": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "@vitest/ui": "catalog:",
    "fallow": "catalog:",
    "fastify": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "fastify": "^5.12.3"
  },
  "engines": {
    "node": ">=24"
  }
}
